package kessel.hex.admin;

/** Gives the details on the selected hex in the map. */
class AdminDetailsPane extends AbstractDetailsPane
{
  AdminDetailsPane( AdminDisplay gameDisplay )
  {
    super( gameDisplay );
  }
}
